import React from 'react';
import "./App.scss"

function App() {
  return (
    <div className="App">
      <h1> Hello World </h1>{' '}
    </div>
     /* <React.Fragment>
    (instead of using div, just use the fragment, inside can add h1, h2 multiple elements)
    </React.Fragment>
     */   
  );
}

export default App;
